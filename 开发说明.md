# 会议室预约系统

## 1. 项目简介
本项目是一个基于 .NET 10 Web API 和 React 19 的全栈会议室预约管理系统。系统采用液态玻璃拟态（Glassmorphism）设计风格，通过渐变背景、毛玻璃卡片和动态光斑营造现代、通透的视觉体验。

### 核心技术栈
- **后端**: ASP.NET Core 10, Entity Framework Core (MySQL), JWT 认证, BCrypt 加密
- **前端**: React 19, TypeScript, Vite 7, React Router v7, Day.js (日期处理)
- **数据库**: MySQL 8.0
- **设计**: 液态玻璃拟态风格（渐变紫粉背景 + 毛玻璃模糊 + 动态变形光斑），原生 CSS 变量系统，响应式布局

---

## 2. 功能模块说明

### 2.1 用户管理
- **用户注册**: 简化的注册流程（用户名+密码），注册后进入”待审核”状态。
- **账号关联**: 注册审核通过后，用户需通过姓名+手机号自动匹配职工档案完成账号关联，未关联者无法使用系统功能。
- **注册审核**: 管理员可在”用户审核”页面查看新注册用户列表，点击”通过”或”拒绝”。
- **身份认证**: 基于 JWT Token 的身份验证，不同角色（Admin/User）拥有不同操作权限和视图。
- **个人中心**: 展示个人信息（支持修改手机号和邮箱）、通知消息列表、即将到来的预约。
- **部门管理**: 管理员专属，支持部门的增删改查。
- **职工管理**: 管理员专属，管理全公司员工信息，支持按部门筛选，并可查看员工是否已开通系统账号。

### 2.2 会议室管理
- **双视图**: 管理员看到”会议室管理”界面（支持增删改），普通用户看到”会议室介绍”界面（只读）。
- **信息维护**: 支持会议室名称、位置、容纳人数、描述等信息的管理。
- **设备标签**: 可为会议室勾选配备的设备（如投影仪、视频会议系统等）。
- **状态切换**: 支持将会议室设为”可用”或”维护中”。维护中的会议室不可被预约。
- **甘特图时段视图**: 点击会议室卡片可查看该会议室一周7天（08:00-23:00）的时段占用甘特图，用不同颜色标识空闲/已预约/待审批/维护中状态。支持”时段总览”查看所有会议室单日概览。

### 2.3 会议室预约 
- **预约流程**: 用户通过”我的预约”页面创建预约，选择会议室、时间段和参会人数提交预约申请。
- **审批流程**: 所有预约默认进入”待审批”状态，由管理员在”预约管理”页面进行审核（通过/拒绝）。
- **冲突检测**: 系统自动检测时间重叠。若同一会议室在同一时间段已被他人预约（非拒绝/取消状态），则无法再次预约。
- **我的预约**: 用户可查看自己的所有预约记录，支持在开始前主动取消预约。管理员无此功能模块。

### 2.4 扩展功能 
- **个人中心**: 集中展示个人信息、通知消息（支持标记已读）和即将到来的预约列表，支持在线修改手机号和邮箱。
- **通知系统**: 用户注册审核、预约结果反馈等关键操作会产生实时通知提醒。未读通知数以红色气泡形式显示在侧边栏"个人中心"按钮右上角。
- **全局搜索与筛选**: 预约管理支持按日期、会议室、状态等多种维度快速查找预约信息。
- **管理/用户双视图**: 管理员侧边栏展示全部管理功能，普通用户仅显示个人中心、会议室介绍、我的预约三项核心功能。

---

## 3. 数据库设计
系统包含 7 张核心数据表：
1. `Departments`: 部门信息
2. `Employees`: 员工档案
3. `Users`: 系统账户信息
4. `Rooms`: 会议室基本资料
5. `RoomEquipments`: 会议室设备关联
6. `Reservations`: 预约申请记录
7. `Notifications`: 系统站内消息

---

## 4. 部署与运行说明

### 4.1 环境准备
- **数据库**: 请确保本地 MySQL 8.0 正在运行。
  - **默认库名**: `conference_room_db`
- **运行环境**: 安装有 .NET 10 SDK 和 Node.js 环境。

### 4.2 运行步骤
1. **自动建库**: 首次运行后端项目时，系统会自动执行 `db.Database.EnsureCreated()`，在您的 MySQL 中创建数据库、表结构，并导入种子数据（管理员账号、初始会议室等）。
2. **启动命令**: 
   - **方式一 (Aspire)**: 运行 `Conference_Room_Reservation_System.AppHost` 项目，将同时启动前后端。
   - **方式二 (手动)**: 
     - 进入 `Conference_Room_Reservation_System.Server` 运行 `dotnet run`
     - 进入 `frontend` 运行 `npm run dev`

### 4.3 演示账号
- **管理员**: `admin` / `admin123`
- **普通用户**: `zhangsan` / `123456`

---

## 5. 设计美学与用户体验
- **色彩方案**: 采用液态玻璃拟态风格（Glassmorphism），渐变紫粉蓝背景（#667eea → #764ba2 → #f093fb），白色半透明毛玻璃卡片（backdrop-filter: blur），金色作为交互强调色。
- **双视图设计**: 管理员与普通用户拥有不同的侧边栏导航和页面功能，确保各自操作聚焦。
- **响应式布局**: 适配电脑、平板及手机屏幕。
- **动态效果**: 背景光斑变形浮动动画（morph + float），侧边栏折叠过渡，模态框弹出动画，卡片悬停抬升效果等，提升操作顺滑感。
